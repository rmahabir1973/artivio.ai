# VPS Video Processor Configuration
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3001
TEMP_DIR=/tmp/video-processing

# FFmpeg 8.0.1 Paths
FFMPEG_PATH=/root/bin/ffmpeg
FFPROBE_PATH=/root/bin/ffprobe

# AWS S3 Configuration (Required)
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_REGION=us-east-1
S3_BUCKET=your-bucket-name

# Callback Security (Recommended)
# Used to sign callbacks with HMAC-SHA256
CALLBACK_SECRET=your_secure_callback_secret

# OpenAI Configuration (Optional - for Whisper transcription)
# If not set, auto-transcription will be disabled
OPENAI_API_KEY=sk-your_openai_api_key
